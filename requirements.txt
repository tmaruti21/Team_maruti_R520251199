# Python dependencies for infer_node.py (custom_follow ROS2 package)
# Install inside the project venv:
#   pip install -r requirements.txt
#
# ROS2 packages (rclpy, sensor_msgs, geometry_msgs, cv_bridge) are provided
# by the ROS2 Humble installation and do NOT need to be pip-installed.

# Computer vision
opencv-python==4.10.0.84

# Deep learning
torch==2.10.0
torchvision==0.25.0

# Numerical
numpy<2                  # NumPy 2.x breaks cv_bridge in ROS2 Humble

# Image processing
Pillow==12.1.1

# ESP32 serial bridge
pyserial>=3.5,<4
